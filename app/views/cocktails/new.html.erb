<h1>Add a new cocktail</h1>

<%= simple_form_for (@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit "Add", class: "btn btn-info" %>
<% end %>
</br>

<p><%= link_to "Cancel", root_path, class: "btn btn-outline-info" %></p>
